from SECRETS import secrets

APP_ID = secrets['app_id']
APP_SECRET = secrets['app_secret']
BAN_PATTERN = secrets['ban_dict']
CHAN = secrets['channel_name']
HOST = secrets['host']
NICK = secrets['username']
PASS = secrets['oauth_token']
PORT = secrets['port']
RATE = secrets['message_per_second']

if __name__ == '__main__':
    pass
